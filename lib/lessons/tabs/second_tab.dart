import 'package:flutter/material.dart';

class SeconsTab extends StatelessWidget {
  const SeconsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pinkAccent,
      child: Center(
        child: Text(
          '2ST TAB',
        ),
      ),
    );
  }
}